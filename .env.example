# CompoundAtlas — Environment Variables
# Copy this to .env and fill in your values

# ─── DATABASE ────────────────────────────────────────
DATABASE_URL="postgresql://atlas:atlas_dev@localhost:5432/compoundatlas"

# ─── REDIS ───────────────────────────────────────────
REDIS_URL="redis://localhost:6379"

# ─── AUTH (NextAuth.js) ──────────────────────────────
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# GitHub OAuth (optional, for login)
GITHUB_ID=""
GITHUB_SECRET=""

# Google OAuth (optional, for login)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ─── RESEARCH APIS (all free) ───────────────────────
# NCBI E-Utilities — Get key: https://www.ncbi.nlm.nih.gov/account/
NCBI_API_KEY=""
NCBI_EMAIL="your-email@example.com"

# Semantic Scholar — Get key: https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=""

# OpenAlex — No key needed, just set your email for polite pool
OPENALEX_EMAIL="your-email@example.com"

# ─── SEARCH (Typesense) ─────────────────────────────
TYPESENSE_API_KEY="compound_atlas_dev_key"
TYPESENSE_HOST="localhost"
TYPESENSE_PORT="8108"
TYPESENSE_PROTOCOL="http"

# ─── AI (OpenRouter) ────────────────────────────────
# Required for AI Stack Builder (/stacks/ai)
# Get key at https://openrouter.ai/keys
OPENROUTER_API_KEY=""
# Optional: override the default model (default: moonshotai/kimi-k2)
# Any OpenRouter model works: openai/gpt-4o-mini, anthropic/claude-3-5-haiku, etc.
# OPENROUTER_MODEL="moonshotai/kimi-k2"

# ─── APP CONFIG ──────────────────────────────────────
NODE_ENV="development"
